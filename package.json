{
  "name": "run-event-registration-poc",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently --kill-others --names backend,public,admin --prefix-colors blue,green,magenta \"npm run dev:backend\" \"npm run dev:public\" \"npm run dev:admin\"",
    "dev:backend": "npm run dev --workspace @run-event/backend",
    "dev:public": "npm run dev --workspace @run-event/public-web",
    "dev:admin": "npm run dev --workspace @run-event/admin-dashboard",
    "build": "npm run build --workspace @run-event/public-web && npm run build --workspace @run-event/admin-dashboard",
    "test": "npm run test:api",
    "test:api": "npm test --workspace @run-event/backend",
    "test:e2e:dev": "npm run test:dev --workspace @run-event/e2e-tests",
    "test:e2e:prod": "npm run test:prod --workspace @run-event/e2e-tests",
    "check": "npm run check:workspaces && npm run test:api && npm run build",
    "check:workspaces": "npm ls --workspaces --depth=0"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
